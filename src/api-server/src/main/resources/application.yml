server:
  port: 8080

# CORS: 배포 시 CORS_ALLOWED_ORIGINS 환경변수로 도메인 지정 (쉼표 구분)
# 예: CORS_ALLOWED_ORIGINS=https://newspaper.example.com,https://dailyfocus.example.com
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://xn--vg1b002am5bc0y3ga.com,https://www.xn--vg1b002am5bc0y3ga.com,http://localhost:5173,http://localhost,http://localhost:8081,http://127.0.0.1,http://127.0.0.1:8081}

spring:
  application:
    name: api-server
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/newspaper?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: ${MAIL_HOST:imap.naver.com}
    port: ${MAIL_PORT:993}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        store:
          protocol: imaps
        imaps:
          ssl:
            enable: true
            trust: "*"

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

cloud:
  aws:
    credentials:
      access-key: ${MINIO_ACCESS_KEY:minioadmin}
      secret-key: ${MINIO_SECRET_KEY:minioadmin}
    s3:
      endpoint: ${CLOUD_AWS_S3_ENDPOINT:http://localhost:9000}
      bucket: ${MINIO_BUCKET:newspaper-bucket}
    region:
      static: ${MINIO_REGION:us-east-1}
    stack:
      auto: false

gemini:
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:gemini-2.0-flash}
  enabled: ${GEMINI_ENABLED:true}

logging:
  level:
    com.newspaper.api_server.service.GeminiService: DEBUG
